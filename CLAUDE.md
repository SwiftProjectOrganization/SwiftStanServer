# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

SwiftStanServer is a non-sandboxed **macOS app** that hosts an HTTP server exposing 13 SwiftStan commands over an OpenAPI interface. It is the execution tier: it owns cmdstan and the `~/Documents/StanCases` filesystem, and clients (notably [`SwiftStanApp`](../SwiftStanApp), and potentially SwiftStats) talk to it over HTTP. The HTTP layer is generated by Apple's [Swift OpenAPI Generator](https://github.com/apple/swift-openapi-generator) with a [Hummingbird](https://github.com/hummingbird-project/swift-openapi-hummingbird) transport. macOS 14+; Swift 6.

Three independent projects, coupled only by contracts:
- `SwiftStanLibrary` — the **proper, distributable** library at `../SwiftStanLibrary` (or remote GitHub URL once published). This is what the server depends on.
- `SwiftStanServer` (this) — links `SwiftStanLibrary` and serves its 13 public commands.
- `SwiftStanApp` (client) — has **no** build dependency on this project; talks to the server purely over HTTP, generating its client from a copy of `openapi.yaml`.

The full design rationale lives in `../SwiftStan/Docs/SwiftStanServer-Plan.md`.

## First-time setup (REQUIRED before the project builds)

This repo currently contains source files, the OpenAPI spec, and entitlements, **but not an `.xcodeproj`** — the Xcode project must be created through the Xcode UI (hand-authoring `project.pbxproj`, especially SPM build-tool-plugin wiring, is fragile and risks crashing a running Xcode). Steps:

1. **Create the project**: Xcode → File → New → Project → macOS → App. Product Name `SwiftStanServer`, Interface SwiftUI, Language Swift, deployment target macOS 14. Save it **into this directory** so the existing `SwiftStanServer/` source folder is reused.
2. **Add the existing files to the target**: `SwiftStanServerApp.swift`, `ContentView.swift`, `ServerSettings.swift`, `ServerController.swift`, `StanAPIHandler.swift`, and the `OpenAPI/` group (`openapi.yaml`, `openapi-generator-config.yaml`). Make sure `openapi.yaml` + the config are **members of the app target** (the plugin scans target membership).
3. **Link the library**: File → Add Package Dependencies → `https://github.com/SwiftProjectOrganization/SwiftStanLibrary`; add the `SwiftStan` product to the target.
4. **Add the OpenAPI packages** (File → Add Package Dependencies — remote URLs, reliable):
   - `https://github.com/apple/swift-openapi-generator` (≈ from 1.6.0) — add the **OpenAPIGenerator** *plugin* only.
   - `https://github.com/apple/swift-openapi-runtime` (≈ from 1.8.0) — product `OpenAPIRuntime`.
   - `https://github.com/hummingbird-project/swift-openapi-hummingbird` (≈ from 2.0.1) — product `OpenAPIHummingbird`.
   - `https://github.com/hummingbird-project/hummingbird` (≈ from 2.5.0) — product `Hummingbird`.
5. **Wire the build plugin**: Target → Build Phases → "Run Build Tool Plug-ins" → **+** → `OpenAPIGenerator`. Trust the plugin when prompted.
6. **Set entitlements**: Target → Signing & Capabilities → point `CODE_SIGN_ENTITLEMENTS` at `SwiftStanServer/SwiftStanServer.entitlements` (non-sandboxed + `network.server`). Enable Hardened Runtime.
7. **Build.** The first build generates the OpenAPI code; then `StanAPIHandler` compiles against the generated `APIProtocol`. Reconcile any idiomatic-naming differences Xcode flags.

## Architecture

### HTTP contract

`OpenAPI/openapi.yaml` is the **canonical** spec (SwiftStanApp keeps a byte-identical copy). **13 operations** (one `POST /v1/<command>` each) plus `GET /v1/health`. `dsl2stan` is **not** included — SwiftStanLibrary dropped it because it requires recompiling the package source tree via `swiftc` (not viable in a distributed library; the in-process `stancode` covers alist→Stan without `swiftc`).

Every command returns `200` with a `CommandResult { status, error, outputPath? }`:
- `status` — the human-readable status line (library tuple `.0`, or `"Wrote <file>"` for file ops).
- `error` — library tuple `.1`; `""` on success. Non-empty = logical failure at `200`. Real `4xx/5xx` reserved for transport faults.
- `outputPath` — set by file-translation ops; omitted for cmdstan ops.

### Two operation families (mirror SwiftStanLibrary)

- **cmdstan-backed** (`compile, sample, optimize, pathfinder, laplace, generated_quantities, stansummary, ulam`) → call library funcs returning `(String, String)`. Requests carry `model`, `arguments` (verbatim cmdstan `key=value`), optional `cmdstan` override, `verbose`, plus per-op flags (`compile`: install/force; `sample`: install/nosummary; `ulam`: force).
- **pure-Swift file translation** (`csv2json, alist2dsl, stancode, stan2alist, runinfo`) → call library funcs that `throw`/return `URL`. Requests carry `model` (required) + `verbose` (+ `force` for `stan2alist`).

### Files

- `StanAPIHandler.swift` — `struct StanAPIHandler: APIProtocol`; one method per operation forwarding to the matching `SwiftStan` function. Synchronous library calls (blocking `Process.run()`) are wrapped in `Task.detached` via `offload(_:)` so the event loop isn't starved.
- `ServerController.swift` — `@MainActor ObservableObject` owning the Hummingbird `Application` lifecycle (`start()`/`stop()`), binding to `127.0.0.1:<port>`.
- `ServerSettings.swift` — cmdstan path (UserDefaults `"cmdstanPath"` → `$CMDSTAN` → hardcoded default), port (UserDefaults `"serverPort"`, default 8080), and reported StanCases root.
- `SwiftStanServerApp.swift` — `@main` App; starts the server on appear.
- `ContentView.swift` — minimal GUI: running indicator, port, cmdstan path, Start/Stop.

### Long-running operations

Calls are **synchronous** (one blocking HTTP request per command — localhost, generous timeouts). cmdstan sampling/compiling can take minutes; the server keeps the response open until the library returns. If timeouts bite, raise Hummingbird's response/idle timeout in `ServerController`.

## Key constraints

- macOS 14+; Swift 6. Non-sandboxed (shells out to cmdstan, writes `~/Documents/StanCases`). Developer-ID / direct distribution.
- Depends on **`SwiftStanLibrary`** (`https://github.com/SwiftProjectOrganization/SwiftStanLibrary`). Do not add a dependency on the original `SwiftStan` package (the one with the CLI) or on `SwiftStanApp`.
- Required server-side env: `CMDSTAN` (or set in the GUI / UserDefaults). `STAN_CASES` optional (defaults to `~/Documents/StanCases`). `SWIFTSTAN_PROJECT_ROOT` is no longer needed (`dsl2stan` is not in the API).
- Keep `openapi.yaml` byte-identical to the copy in SwiftStanApp (canonical spec, copied).

## Verification

1. Build in Xcode (after first-time setup). Confirm the plugin emitted `APIProtocol`/`Types` and `StanAPIHandler` compiles.
2. Run; the GUI shows "Running on http://127.0.0.1:8080". `curl http://127.0.0.1:8080/v1/health` → JSON with resolved cmdstan + StanCases paths.
3. `curl -XPOST http://127.0.0.1:8080/v1/stancode -H 'Content-Type: application/json' -d '{"model":"bernoulli"}'` → `{"status":"Wrote bernoulli.stan","error":"","outputPath":"..."}`.
4. `curl -XPOST .../v1/compile -d '{"model":"bernoulli"}'` then `.../v1/sample` → `error:""` and `bernoulli.samples.csv` appears under `~/Documents/StanCases/bernoulli/Results/`.
